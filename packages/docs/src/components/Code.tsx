import { hope } from '@hope-ui/solid'

const Code = hope('code', {
  baseStyle: {
    color: '$primary11',
    fontFamily: '$mono',
    fontSize: '0.95em',
    padding: '3px 6px',
    'background-color': 'var(--vp-c-bg-mute)',
    'border-radius': '4px'
  }
})

export default Code
